export interface Region {
  code: string;
  name: string;
}

export interface SelectedLocation {
  provinceCode: string;
  provinceName: string;
  cityCode: string;
  cityName: string;
  districtCode: string;
  districtName: string;
  villageCode: string;
  villageName: string;
}
